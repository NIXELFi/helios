# 0004 — junction-kind IMEP sensitivity (SDM25 / characteristic / RPM sweep)
# Companion to study_sdm26_char.toml — see that file for narrative. SDM25
# is the pre-Phase-F tune; included per spec C10 two-calibration baseline.

[run]
config = "crates/engine-sim/python_ref/configs/sdm25.json"
rpm = [6000.0, 7000.0, 8000.0, 9000.0, 10000.0, 11000.0, 12000.0, 13000.0]
cycles = 30
recorded = true
seed = 4000
junction = "characteristic"

[environment]
target_triple = "aarch64-apple-darwin"
rustc_version = "rustc 1.95.0"
rayon_threads = 1
libm_source = "system"

[[acceptance]]
metric = "brake_power_kW@10000"
target = 46.5
tolerance = "±15%"
citation = "references/dyno/cbr600rr-fsae-restricted.csv row rpm=10000"

[[acceptance]]
metric = "brake_power_kW@13000"
target = 50.5
tolerance = "±15%"
citation = "references/dyno/cbr600rr-fsae-restricted.csv row rpm=13000"
